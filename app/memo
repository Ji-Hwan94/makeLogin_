이곳은 제가 수업을 들으면서 하나하나 정리한 것들입니다.


1) 서버를 끄기 위해서는 terminal 에서 ctrl c 를 누룬다.

2) ! + enter 를 치면 html 기본 양식이 완성된다.

3)module.exports = (method의 이름)
은 무언가를 내보내는 함수이다. 

4) const 변수 = require("경로")는 경로에 있는 함수를 받아서 사용하겠다는 것을 표현하는 method다

5) reduce: 배열의 값을 줄여서 하나의 값으로 만드는 method,
    숫자든, 문자든 하나의 값으로 만드는 것 (value, array, object 다 가능!!!!)